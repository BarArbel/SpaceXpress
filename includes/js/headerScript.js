$( document ).ready(function() {
    var jason_data[];
    var user = document.getElementById("user");

    $$.getJSON("data/users.json", function (data) {
        jason_data = data;

        for(row of data){
            if(row.user_id == )
        }
    });
});